<div id="page-wrapper">
    <ul id="peeps">
        <% @users.each do |user| %>
            <% if user.photo? %>
                <li>
                    <%= image_tag(user.photo, :alt => user.name, :class => "index-photos ui-droppable", "data-name" => user.name) %>
                </li>
            <% end %>
        <% end %>
    </ul>
</div>
<div id="game-sidebar">
    <%= render :partial => "users/game/names_sidebar" %>
</div>